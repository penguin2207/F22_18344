TEST_TOOL_ROOTS := memory-hierarchy 
TEST_ROOTS :=
SANITY_SUBSET :=
TOOL_ROOTS :=
SA_TOOL_ROOTS :=
APP_ROOTS :=
OBJECT_ROOTS :=
DLL_ROOTS :=
LIB_ROOTS :=

TOOL_CXXFLAGS += -std=c++11 -I.
TOOL_OBJECTS = \
	$(OBJDIR)memory-hierarchy$(OBJ_SUFFIX) \
	$(OBJDIR)cache$(OBJ_SUFFIX)

$(OBJDIR):

$(OBJDIR)memory-hierarchy$(PINTOOL_SUFFIX): $(TOOL_OBJECTS)
	$(LINKER) $(TOOL_LDFLAGS) $(LINK_EXE)$@ $(TOOL_OBJECTS) $(TOOL_LPATHS) $(TOOL_LIBS)
